<div class="container">
  <% @user.followed_users.each do |followed_user| %>
      <div class="container-child col-md-2 col-sm-4 col-xs-6 ">
        <%= profile_img(followed_user) %>
        <%= followed_user.name %>
        <%= render 'follow_form', user: followed_user %>
      </div>
  <% end %>
</div>
